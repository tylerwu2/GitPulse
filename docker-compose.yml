services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: etl
      POSTGRES_PASSWORD: etl
      POSTGRES_DB: etl_db
    ports:
      - "5432:5432"
    volumes:
      - ./sql/pr_schema.sql:/docker-entrypoint-initdb.d/pr_schema.sql